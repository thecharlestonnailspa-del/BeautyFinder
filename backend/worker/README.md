# Worker

Background processing app for:

- booking reminders
- notification delivery
- message side effects
- moderation queues

Current local implementation:

- fetches owner notifications from the mock API
- verifies worker-to-API connectivity
- builds to `dist/`
